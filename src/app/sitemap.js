export default function sitemap() {
  return [
    {
      url: "https://faheem506pk.vercel.app",
      lastModified: new Date(),
      changeFrequency: "weekly",
      priority: 1,
    },
    // Add other routes if valid, e.g. /projects if it exists as a separate page
  ];
}
